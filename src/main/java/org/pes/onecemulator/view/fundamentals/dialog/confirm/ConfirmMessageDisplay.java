package org.pes.onecemulator.view.fundamentals.dialog.confirm;

import com.vaadin.ui.Label;

class ConfirmMessageDisplay extends Label {

    private static final String ID = "ConfirmMessageDisplay";

    ConfirmMessageDisplay() {
        setId(ID);
        setSizeFull();
    }
}
